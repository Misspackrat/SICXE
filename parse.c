//parse function, takes in word by word. Returns length and updates pointer given to it
#include <stdbool.h> 
#include <stdio.h> 
#include <string.h> 
#include <stdlib.h> 

int parse(char *w){
    int count = 0;


    return count;
}
